export type ImportType = "named" | "namespace" | "default";

export const ImportType = {
    Named: "named" as ImportType,
    Namespace: "namespace" as ImportType,
    Default: "default" as ImportType
};
